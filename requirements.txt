# Requires Python 3.11 or 3.12 (PyTorch, pulled in by sentence-transformers,
# does not yet publish wheels for 3.13+). See .python-version.

# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# Retrieval / embeddings
sentence-transformers>=3.1
numpy>=1.26

# Document loading
pypdf>=5.0

# LLM providers
anthropic>=0.39
# openai>=1.51  # uncomment if you set LLM_PROVIDER=openai

# UI
streamlit>=1.38

# Dev / eval
pytest>=8.3
httpx>=0.27
